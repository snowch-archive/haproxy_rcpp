Run the following commands from project's parent folder:

* `R CMD build Haproxy` to build the package tarball.
* `R CMD check Haproxy` to check the package tarball.
* 'R CMD install Haproxy' to install the package tarball.

* R CMD INSTALL --preclean --no-multiarch --with-keep.source Haproxy

